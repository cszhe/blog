title: Zongjian's blog
description: Personal blog of Zongjian He
url: "https://www.hezongjian.com"

author:
  name: Zongjian He
  email: ""

timezone: America/New_York

plugins:
  - jekyll-feed
  - jekyll-remote-theme
  - jekyll_slugify

exclude:
  - "Gemfile"
  - "Gemfile.lock"
  - "README.md"
  - "Makefile"
  - "fabfile.py"
  - "pelicanconf.py"
  - "publishconf.py"
  - "requirements.txt"
  - "Dockerfile"
  - "external/"
  - "deploy/"
  - "tasks.py"
  - "content"
  - ".bundle"
  - ".jekyll-cache"
  - "vendor"

# Preserve old Pelican URLs - use same slug format
permalink: /:title.html

# Keep uploads in root directory
include:
  - "uploads"

# Minima theme via remote theme
remote_theme: jekyll/minima

# Minima theme settings
minima:
  date_format: "%B %-d, %Y"
  show_excerpts: true

# Pagination settings
paginate: 10
paginate_path: "/page/:num/"

# Slugify settings
slugify: none
